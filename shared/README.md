# coreview-shared

Internal library shared by the Nexo Co-Review **backend** and **agent** packages.

Contains provider protocols, GitHub Git/CI implementations, runtime specs, callback schemas,
and OpenCode config builders. Not a deployable system component.
